node_modules/
.next/
dist/
.turbo/
.env*
!.env.example
.DS_Store
.vercel

# TypeScript incremental build cache
*.tsbuildinfo

# Raw upstream imports kept locally as source material for original rewrites.
# Not published: they carry another project's attribution and the catalogue is
# now entirely our own work.
import-staging/

# Video capture frames and renders (31MB of build artifacts)
.video/
